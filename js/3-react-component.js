/*
react is all about component

we can create 1 component with 1 tag or we can create a component with a group of 5-6 tag


Component are reusable building blocks in user ineterface(html css and js makes a component).

in react we build component with html css js.


and then by combining these component we built a page.
*/